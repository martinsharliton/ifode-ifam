<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cardapio" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Defina uma cor primária explícita -->
        <item name="colorPrimary">@color/primary</item>

        <!-- Adicione colorOutline para a cor da borda do outline -->
        <item name="colorOutline">@color/white</item>

        <!-- Defina colorOnSurfaceVariant para adaptar o texto e a borda -->
        <item name="colorOnSurfaceVariant">@color/white</item>
    </style>

    <style name="Theme.Cardapio" parent="Base.Theme.Cardapio" />
</resources>
