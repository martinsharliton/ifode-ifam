<!-- res/drawable/dialog_rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Cor de fundo do diálogo -->
    <corners android:radius="16dp" /> <!-- Raio das bordas arredondadas -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
    <stroke android:width="1dp" android:color="@color/gray" /> <!-- Borda opcional -->
</shape>
